本文档记录方向性目标，请保持简洁（一行简单目标，不过三行说明）

待建设:
    多媒体功能支持
        当前目标为简便
    油猴功能模仿
        提供和维护相关接口
        维护相关能力，并关注ai agent关联发展情况
    自定义化的工具
        为用户脚本增加特殊grant记录，用以标注为工具和相关参数，并在内核中完成自动注册
        用户脚本通常需要在main world中执行
        完成后大模型将有能力引入和开发维护外部工具
    请求编排
        在已有能力基础上，加入身份与使命（不允许运行中修改）、记忆与记忆管理（作为tool引入）
        记忆管理与上下文自动截断结合
        缓存机制开发

长远计划（价值有，但不是当前优先开发目标）：
    隐私保护与内核隔离
    权限管理