## 2026-07-01

### 清理 Kernel 中 log 对象残留
- **Kernel.ts**: 移除 `log: ILogger | null` 属性、`ILogger` import、构造函数 log 选项、`this.log?.destroy()` 改为 `Log.getLogger()?.destroy()`、getInfo() 中 `hasLog` 改为固定 `true`
- **SessionManager.ts**: 移除 `log: ILogger | null` 属性声明和 `this.log = obj?.log || null` 赋值
- **SettingsManager.ts**: 移除 `log: ILogger | null` 属性声明和 `this.log = obj?.log || null` 赋值
- **index.ts**: 移除 `ILogger` import 和 export（TS interface 编译擦除，无外部消费者）
- 构建通过: 131.60 kB
- 补充: app.js Kernel 构造函数调用中遗留的 `log: null` 参数也已移除

### UI 现代化改造方案
- 完成 UI 架构深度分析（37 文件、8,200 行代码），识别出全量 DOM 重建、全局 CSS 冲突、无类型安全等核心问题
- 用户选择 Svelte 5 作为目标框架，编写完整迁移方案文档 docs/svelte-migration-plan.md
- 6 阶段迁移路线：基础建设 → 组件重构 → SettingsPage → 列表页 → ChatPage → 清理
- 新目录 src/ 包含 pages/、components/、stores/、lib/；保留变量 CSS 作为全局主题

### Kernel 全面审查 + 20 项修复
构建通过: 130.07 kB (59 modules)

**严重问题修复 (3):**
- ChatProgram.ts: `createSession()` 未 await → 加 await，修复 session.id undefined
- OpenRouterService.ts: 日志打印含 API Key 的完整请求体 → 改为仅记录元数据
- ProviderFactory.ts: `off()` 只传事件名未传回调 → 存储回调引用，destroy 时精确移除

**高级问题修复 (5):**
- Kernel↔ScriptsManager 循环依赖 → ScriptsManager 用 KernelRef 接口替代 Kernel import
- Kernel↔ProviderFactory 循环依赖 → ProviderFactory 用 KernelRef 接口
- I* 命名 (class not interface): ISessionManager/ISettings/IProviderAPIService/IScriptsManager → 重命名为 Base* 前缀 (12 文件)
- Events 双重定义: sidepanel/js/events.js → 从 kernel/Events.ts 统一导入
- Kernel/ChatProgram extends Process 冗余 → 移除继承，直接声明 name 属性

**中等问题修复 (6):**
- ChatProgram _onSessionChangedHandler 空方法 → 删除，逻辑内联
- SettingsManager 4 个 stub 方法 → 合并为一行注释
- CapabilityManager _audit 死代码 → 在 check/require 中调用审计
- SettingsManager 无意义注释 (3处) → 删除
- OpenRouterService window.location.href → 加 FIXME 注释
- ChatProgram chrome.tabs.query → 加 FIXME 注释

**低优先级修复 (6):**
- OpenAIService 未使用的 buffer 变量 → 删除
- OpenAIService 静默 catch(e){} → 加注释说明
- Settings.toJSON() apiKey 明文 → 脱敏为 '***'
- ChatProgram 硬编码模型名正则 → 加注释说明
- ChatProgram 魔法数字 50ms → 加注释说明
- ChatProgram kernel: null 类型不一致 → 显式类型转换

### Svelte 5 Phase 1 基础设施建设 — 完成
- 安装 svelte 5.56.4 + @sveltejs/vite-plugin-svelte 7.1.2
- Vite 配置改为双入口：sidepanel.bundle.js (旧UI) + svelte-app.bundle.js (新UI)
- 创建 svelte.config.mjs (因 package.json type: commonjs)
- 创建 src/ 完整目录结构: main.ts (Kernel 自举启动) → App.svelte (根路由) → 5 个页面占位桩
- Kernel context 注入: provideKernel/useKernel via Svelte Context API
- Sidebar 组件: Svelte 5 runes ($props/$state/$effect)，自适应高亮路由
- 新 HTML 入口: sidepanel/svelte-app.html (独立于旧 UI)
- tsconfig.json 添加 src/ 路径映射
- 修复构建时发现的 2 个语法 bug:
  - OpenRouterService.ts: listModels() 方法体缺少闭合 }
  - LMStudioService.ts: .then 链缺少闭合 }
- 构建通过: sidepanel.bundle.js 70.88 kB + svelte-app.bundle.js 49.09 kB + shared chunk 64.28 kB
- 5/5 测试通过，无回归

### 全面补日志 — 网络层强制 + 关键UI交互
用户要求：关键 UI 交互 + 网络交互必须打日志。

**P0 网络层 (3 文件):**
- **OpenAIService.ts**: 新增 Log import → chat() 请求开始/成功/HTTP错误/网络异常全量日志；chatStream() 请求开始/成功(chunk数)/SSE解析失败warn/取消/网络异常；cancel() 加日志
- **OpenRouterService.ts**: chatStream() 请求开始/成功(内容长度+finishReason)/HTTP错误/catch异常日志；listModels() 请求开始/成功(模型数)/HTTP错误日志
- **LMStudioService.ts**: chat() 请求开始/成功/取消/HTTP错误/异常日志；chatStream() 请求开始/成功/取消/HTTP错误/异常日志；listModels() 成功日志(含端点和模型数)

**P1 关键 UI (8 文件):**
- **ChatEventHandler.js**: _handleApplySend 正常路径加 info(内容长度+reasoningEffort)；_handleApplyStop 加 info；_handleApplyDeleteMessage 正常路径加 info；_handleToolExecuting/Completed 加 debug(toolCallId+状态+耗时)
- **ChatPage.js**: 新增 Log import；"+ 新对话"按钮加 info；工具切换加 info(name→状态)；停止按钮加 debug；checkModelSupportsThinking 从 kernel.log 改为 Log 单例
- **HistoryPage.js**: loadConversation 加 info(id)；deleteConversation 加 info(请求+已删除)
- **SettingsEventHandler.js**: API_STANDARD_CHANGED/MODELS_REQUEST/SETTINGS_UPDATED/confirmReloadModels 全加 info
- **SettingsPage.js**: handleThemeChange 加 info(theme)；selectModel 加 info(modelId)
- **ScriptsEventHandler.js**: handleInstall(info含codeLength)/handleToggle(info+id→状态)/handleDelete(info+id)/handleEdit(info+id+codeLength)
- **StorageEventHandler.js**: handleRefresh(info)/handleSearch(info)；清理 handleDelete 中3条调试日志(Toast对象dump)
- **ManageUserScriptsTool.js**: 新增 Log import；5个action case 全加 info；default 加 warn
- **RunUserScriptTool.js**: 执行开始加 info(tabId)；成功加 info；失败日志已有

构建通过: 179 modules → sidepanel.bundle.js 70.87 kB
